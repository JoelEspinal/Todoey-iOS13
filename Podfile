# Uncomment the next line to define a global platform for your project
platform :ios, '17.4'

target 'Todoey' do
  # Comment the next line if you don't want to use dynamic frameworks
  #use_frameworks!

  # Pods for Todoey

	pod 'RealmSwift', '~> 10.49', :modular_headers => true
	pod 'Realm', '~> 10.49', :modular_headers => true

end
